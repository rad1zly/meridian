# Dependencies
node_modules/

# Environment & secrets
.env
.env.*
!.env.example
.envrypt

# User config (contains RPC URL, wallet key, API keys)
user-config.json

# GMGN fee-source config (contains API key)
gmgn-config.json

# Stray Claude Code worktrees
.claude/worktrees/

# Runtime state (position tracking, lessons — user-specific)
state.json
lessons.json
smart-wallets.json
pool-memory.json
token-blacklist.json
strategy-library.json
decision-log.json
hivemind-cache.json
signal-weights.json

# Sanitized versions — safe to push (secrets redacted, addresses pseudonymized).
# These mirror the production files above so anyone can see real config structure
# without exposing wallet keys or trade-specific data.
!user-config.sanitized.json
!state.sanitized.json
!lessons.sanitized.json

# Logs
logs/

# OS
.DS_Store

# Dev context
context.md
CONTEXT.md
dev-blocklist.json
docs/

# Discord listener queue
discord-signals.json

# Playwright MCP browser logs
.playwright-mcp/

# Claude Code runtime lock
.claude/scheduled_tasks.lock
